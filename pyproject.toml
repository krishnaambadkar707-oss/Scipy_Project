[project]
name = "ecg-signal-analyzer"
version = "1.0.0"
description = "Synthetic ECG signal analyzer powered by SciPy"
requires-python = ">=3.10"

[tool.uvicorn]
app = "api:app"
host = "0.0.0.0"
port = 8000
